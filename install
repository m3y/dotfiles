#!/bin/bash

set -e

BASEBACKUPDIR="${HOME}/.dotfiles.bak"
BACKUPDIR=${BASEBACKUPDIR}/`date +%Y%m%d%H%M%S`
CURRENT=`pwd`
LISTFILE='target_list'

mkbakdir() {
  [ -d ${BASEBACKUPDIR} ] || mkdir ${BASEBACKUPDIR}
  [ -d ${BACKUPDIR} ] || mkdir -p ${BACKUPDIR}
}

backup() {
  local TARGET=$1
  mkbakdir
  local DIRNAME=`dirname ${BACKUPDIR}/$1`
  if [ ${BACKUPDIR} != ${DIRNAME} ]; then
    mkdir -p ${DIRNAME}
  fi
  mv ${HOME}/$1 ${DIRNAME}
}

exists() {
  local TARGET=$1
  local TYPE=`file -b ${TARGET}`
  if [ "${TYPE}" == "directory" ]; then
    [ -d ${HOME}/${TARGET} ]
  else
    [ -f ${HOME}/${TARGET} ]
  fi
}

# main
echo -e "\n  Install the following files."
while read FILE; do echo "    "${FILE}; done < ${LISTFILE}
echo ""
read -p "  OK? (y/N): " yn 

if [ "y" != "${yn}" ]; then
  exit 1
fi

while read FILE; do
  echo "===== ${FILE} ====="
  exists ${FILE} && backup ${FILE} && echo "${FILE} is backup"
  cp -r ${CURRENT}/${FILE} ${HOME}/${FILE} && echo ${FILE}" ok"
done < ${LISTFILE}
